export enum Overlay {
  EditWorklog = 'editWorklog',
  Settings = 'settings',
  Search = 'search',
}

export enum SidebarLayout {
  Normal = 'normal',
  Compact = 'compact',
  Micro = 'micro',
}

export const LOADING_SPINNER_SPEED = 200;
